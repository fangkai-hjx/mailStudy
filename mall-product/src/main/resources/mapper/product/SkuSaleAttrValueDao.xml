<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.scut.mall.product.dao.SkuSaleAttrValueDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.scut.mall.product.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>
    <select id="getSaleAttrsBySpuId" resultType="cn.scut.mall.product.vo.SkuItemVo$SkuItemSaleAttrVo">
        SELECT attr_name attr_name,attr_id attr_id,GROUP_CONCAT(DISTINCT ssav.attr_value) attr_values FROM `pms_sku_info` si
        LEFT JOIN `pms_sku_sale_attr_value`  ssav ON ssav.sku_id = si.sku_id
        WHERE si.spu_id = #{spuId}
        GROUP BY attr_name,attr_id
    </select>
    <select id="getSkuSaleAttrValuesAsString" resultType="java.lang.String">
        SELECT CONCAT(attr_name,"：",attr_value) FROM `pms_sku_sale_attr_value` WHERE sku_id=#{skuId}
    </select>


</mapper>